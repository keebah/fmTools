import { Role } from "../../types/role";

const ASMun: Role = {
  primary: ["Passen"],
  secondary: ["Abschluss"],
  physis: "mittelfeld",
};

const ASMan: Role = {
  primary: ["Passen"],
  secondary: ["Abschluss"],
  physis: "mittelfeld",
};

export const ASM = {
  ASMun,
  ASMan,
};
